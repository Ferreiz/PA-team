<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Panel Admin</title>
    <!-- Add your CSS links here -->
</head>
<body>
    <h1>Panel Admin - Tạo Tài Khoản Mới</h1>
    <% if (success) { %>
        <p style="color: green;">Tài khoản đã được tạo thành công!</p>
    <% } %>
    <% if (error) { %>
        <p style="color: red;">Lỗi: <%= error === 'missing' ? 'Thiếu thông tin' : 'Tài khoản đã tồn tại' %></p>
    <% } %>
    <form method="POST" action="/register">
        <label for="username">Tên đăng nhập:</label>
        <input type="text" id="username" name="username" required><br>
        <label for="password">Mật khẩu:</label>
        <input type="password" id="password" name="password" required><br>
        <label for="displayName">Tên hiển thị:</label>
        <input type="text" id="displayName" name="displayName" required><br>
        <label for="position">Chức vụ:</label>
        <input type="text" id="position" name="position" required><br>
        <button type="submit">Tạo Tài Khoản</button>
    </form>
</body>
</html>